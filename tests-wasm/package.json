{
  "name": "ankurah-tests-wasm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd bindings && wasm-pack build --target web --out-dir pkg",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/browser": "^2.1.0",
    "playwright": "^1.48.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}


