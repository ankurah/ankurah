# Structured Errors Tasks

## Phase 1
- Add `error-stack` dep
- Add context types
- Add `Failure` + `.diagnostic()` to public enums
- Apply to “events not found” path

## Phase 2
- Validate diagnostic output
- Ensure wasm/uniffi compatibility

## Phase 3
- Split PRs (logging vs repro)
