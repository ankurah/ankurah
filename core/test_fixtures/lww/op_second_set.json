{
  "decoded_data": {
    "cleared": null,
    "count": {
      "I32": 2
    }
  },
  "encoding": "bincode 1.3 `serialize` defaults: fixed-width integers, little-endian, u64 sequence/string length prefixes, u32 enum variant tags, 1-byte Option tag, no length prefix on fixed-size arrays",
  "envelope": {
    "data_len": 46,
    "data_length_prefix_offset": 1,
    "data_length_prefix_width_bytes": 8,
    "data_offset": 9,
    "type": "LWWDiff { version: u8, data: Vec<u8> }",
    "version": 1,
    "version_offset": 0,
    "version_width_bytes": 1
  },
  "fixture": "lww/op_second_set.bin",
  "note": "Carries only `count` and `cleared` - `name` was committed by the previous to_operations() and is absent. A port that resends the whole map produces a longer buffer with different bytes.",
  "produced_by": "LWWBackend::to_operations()[0].diff",
  "scenario": [
    "... op_first_set",
    "set(\"count\", Some(I32(2)))",
    "set(\"cleared\", None)",
    "to_operations()"
  ],
  "total_len": 55
}
